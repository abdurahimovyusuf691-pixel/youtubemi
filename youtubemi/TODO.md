

**Instructions:**
1. Install Git: Download from https://git-scm.com/download/win ^& install, choose "Use Git from Windows Command Prompt"
2. Double-click `upload_to_github.bat` in youtubemi/ folder (it will init, add, commit, push)
3. If repo not exist, create empty repo on GitHub first:

